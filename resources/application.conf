ktor {
    deployment {
        port = 8080
        port = ${?PORT}
        watch = [ classes ]
    }
    application {
        modules = [ konstantin.bezzemelnyi.ApplicationKt.module ]
    }
    development = true
}
